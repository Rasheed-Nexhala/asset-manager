Learn from `/Applications/React/Nexhala/asset-manager/asset-manager/.cursor/skills/react-native-firebase` how to implement Firebase Storage for file operations (upload images, documents, files, download, delete, get URLs, manage metadata, etc.).
Then understand the file handling requirements, implement file upload with progress tracking, handle different file types (images, documents, videos), setup proper file naming and organization, and implement error handling for network issues.
Use two subagents:
1. To understand file storage requirements and setup Firebase Storage configuration with proper security rules
2. To implement file upload/download methods with progress tracking, file compression (for images), and proper error handling