Learn from `/Applications/React/Nexhala/asset-manager/asset-manager/.cursor/skills/react-native-testing-library` and unit test the functions or methods using Jest testing and behavioral test the component using react native testing library. Note that the test maybe newly written or could be adding new missing tests to existing test file for a component.
Use 1 subagent to do setup the dependencies and setup the initial setup needed for a test file and only when that is done then use another subagent to write unit test using jest and another subagent to look into the behavior and write behavioral test. Make sure you add a simple 1 line comment on why that test is done and what it is testing.