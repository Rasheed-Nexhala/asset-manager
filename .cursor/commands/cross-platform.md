Treat **$ARGUMENTS** as a feature or domain (e.g. purchase orders, PO, requests, inventory, auth, vendors, maintenance, sites, activity log, notifications). Follow the **ciams-dual-platform-context** project skill: resolve the domain, then list the canonical **mobile** (`asset-manager/src/...`), **web** (`asset-manager-web/src/...`), and **shared** (`types/`, `services/firebase/`, `store/`, `utils/` under each package) paths as clickable workspace links. Verify uncertain paths with search. If the user only asked for one platform, still mention the other app’s parallel paths briefly so they can opt in. After that, you are prepared to answer questions or implement changes with full cross-platform awareness—do not invent APIs or files.
